import { GmapDirectionsDirective } from './gmap-directions.directive';

describe('GmapDirectionsDirective', () => {
  it('should create an instance', () => {
    const directive = new GmapDirectionsDirective();
    expect(directive).toBeTruthy();
  });
});
